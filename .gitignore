# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo

# Next.js (next-env.d.ts is committed so typecheck works without a prior `next build`)
.next/

# env & secrets
.env
.env.*
!.env.example

# local databases
*.sqlite
*.sqlite3
*.db

# test / coverage
coverage/

# editor / OS
.DS_Store

# Claude Code local settings (personal)
.claude/settings.local.json
