# Copy to .env and fill in. .env is gitignored.
ANTHROPIC_API_KEY=sk-ant-...
# Persistence: sqlite (v1 default) or postgres. Query code is identical across both.
DB_DIALECT=sqlite
# sqlite: a file path. postgres: a connection string.
DATABASE_URL=./lighter.sqlite
